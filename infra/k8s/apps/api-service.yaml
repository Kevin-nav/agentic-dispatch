apiVersion: v1
kind: Service
metadata:
  name: agentic-dispatch-api
  namespace: agentic-dispatch
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agentic-dispatch-api
  ports:
    - name: http
      port: 3000
      targetPort: http
