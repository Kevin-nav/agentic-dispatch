apiVersion: v1
kind: Service
metadata:
  name: t3-code-server
  namespace: agentic-dispatch
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: t3-code-server
  ports:
    - name: http
      port: 3773
      targetPort: http
