apiVersion: v1
kind: Service
metadata:
  name: agentic-dispatch-web
  namespace: agentic-dispatch
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agentic-dispatch-web
  ports:
    - name: http
      port: 80
      targetPort: http
