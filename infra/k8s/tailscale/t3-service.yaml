apiVersion: v1
kind: Service
metadata:
  name: t3-code-server-ts
  namespace: agentic-dispatch
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/tags: "tag:agentic-dispatch-t3"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: t3-code-server
  ports:
    - name: http
      port: 3773
      targetPort: http
