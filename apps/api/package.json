{
  "name": "@agentic-dispatch/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @agentic-dispatch/shared build && vitest run --exclude dist/** --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentic-dispatch/shared": "workspace:*",
    "@octokit/auth-app": "^8.2.0",
    "@octokit/rest": "^22.0.1",
    "convex": "^1.38.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
